// Copy to night-password.local.js and replace the empty string locally.
// An empty password leaves the original two schedules unchanged.
// Never commit night-password.local.js or a real password in this example.
gNightGatePassword = "";
