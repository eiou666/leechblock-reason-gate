# 夜间密码规则（可选，本机配置）

在 1.7.3.6 基础上增加了一次性配置迁移。有本机密码文件时，重新加载「访问网络」后自动应用，不需要重新安装或导入公开的默认配置。仓库不附带真实密码；未配置密码的新安装仍使用原两组理由门。

## 首次启用或迁移到另一台电脑

1. 在 `leechblock-shared` 中将 `night-password.example.js` 复制为 `night-password.local.js`。
2. 仅在这个本机文件中，将 `gNightGatePassword = "";` 的空字符串改为自己的密码。使用合法的 JavaScript 字符串；引号、反斜杠等特殊字符需要转义。不要修改或提交示例文件中的密码。
3. 在 Chrome 扩展管理页面重新加载「访问网络」，然后确认原第 2 组的时段缩短，并新增一个已启用的夜间密码组。

配置迁移只执行一次，之后更改密码应在扩展选项的夜间组中修改并保存；修改本机初始化文件不会覆盖已经迁移的设置。已有两组理由门的用户，以及第一次安装使用默认配置的用户均适用。若第 2 组已经改为其他类型的门禁或已达到分组上限，应先检查配置，不要盲目导入默认文件。

## 启用后的规则

| 分组 | 时间 | 访问条件 | 放行 |
| --- | --- | --- | --- |
| 原第 1 组 | 07:00–11:50、12:00–17:50、18:00–22:00 | 原理由门与 5 秒等待 | 全局 30 分钟 |
| 原第 2 组 | 06:00–07:00、11:50–12:00、17:50–18:00、22:00–23:00 | 原理由门与 5 秒等待 | 全局 5 分钟 |
| 新夜间组 | 23:00–次日 06:00 | 本机配置的密码；不再要求理由或等待 | 全局 5 分钟 |

密码不写入本文、不上传 GitHub。当前用户本机密码来源为 `Code.txt`，文件末尾换行不视为密码的一部分；扩展不会自动读取桌面文件。网站列表沿用原第 2 组；原第 1 组、其他设置以及额外自建组保留。默认新增第 3 组，如果该编号已被使用则追加到后面。

23:00、06:00 切换规则时旧放行终止。密码放行可跨午夜，新标签与同组其他域名共享剩余时间，不重新计时。

本机私有文件 `leechblock-shared/night-password.local.js` 已被 Git 忽略并从发布清单排除。它保存首次迁移所用的明文密码，不应上传。配置迁移后，密码也会按 LeechBlock 原有方式保存于扩展设置中；启用浏览器同步存储时可能参与同步，共享放行的配置签名也包含密码配置。不要导出包含密码的设置并公开分享，也不要在此使用其他账号的重要密码。这不是防篡改的安全系统。

公共 `config/default-options.txt` 仍是无密码的上游备份；不要导入它来恢复本机夜间规则。当前夜间配置应在扩展中单独备份，并注意密码导出开关。

更新本仓库时保留已有设置和本机密码文件；重新加载扩展后临时放行会清除。若改为覆盖其他不含夜间功能的上游代码，需要先备份并重新应用夜间补丁。不要把含真实密码的私有文件加入仓库。
