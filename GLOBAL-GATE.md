# 全局共享放行 · 1.7.3.6

本地扩展让同一 Block Set 内的网站共享固定的放行截止时间。理由页随扩展一起加载，不依赖 Python、8765 端口或计划任务。完整安装和日常命令见 [README](README.md)。

## 默认规则

| Block Set | 每天生效时段 | 每次全局放行 |
| --- | --- | --- |
| 1 | 07:00–11:50、12:00–17:50、18:00–22:00 | 30 分钟 |
| 2 | 00:00–07:00、11:50–12:00、17:50–18:00、22:00–24:00 | 5 分钟 |

覆盖抖音、B站、YouTube、X/Twitter、知乎及配置中的子域名和短链接。完整可导入配置见 [default-options.txt](config/default-options.txt)。

两组均要求至少 5 个非空白字符的理由。在同一页等待 5 秒后手动确认；等待期间按钮禁用，没有数字倒计时，等待结束不会自动放行。刷新理由页会重新等待。

两组 Advanced Options 中的 `Show countdown timer for this block set` 默认关闭。网页不显示剩余时间计时框，后台仍继续计时。分组开关对该组计时角标的作用沿用原插件逻辑；悬停提示和全局显示选项保持原有逻辑。

## 共享时限

- 同组内全部网站、子域名、标签页和窗口共享同一个截止时间。
- 计时按固定截止时间进行；标签不活跃或窗口最小化不会暂停。
- 从地址栏、书签、站内链接或新窗口进入网站，不会补满放行时长。
- 到期后，已打开的页面和新访问都会再次门禁。实际拦截时机受浏览器调度影响。
- 切换时段分组时，旧组放行立即结束。例如 11:40 的放行最多持续到 11:50。
- 午夜仍属于同一组，23:59 的 5 分钟放行可以持续到次日 00:04。
- 后台工作线程休眠保留放行；重启浏览器、重载或更新扩展清除放行。
- 普通窗口和无痕窗口分别计时。使用无痕窗口时需允许扩展在无痕中运行。
- 修改分组配置会使对应旧放行失效；临时锁定仍优先。
- 弹出菜单的 `Discard remaining time` 可结束当前组的共享放行。

## 更新到当前版本

1. 在仓库目录执行 `git pull`。
2. 在 `chrome://extensions` 对本地版点击“重新加载”。
3. 刷新已打开的选项页、网站和理由页。

从尚未迁移的版本升级时，会一次性关闭已有配置中仍使用本机理由门的第 1、2 组倒计时开关，并保存迁移标记。网站、时段、放行时长、其他分组及已保存的全局显示选项保持原样。迁移后手动重新开启显示的选择会被保留。

当前版本会停止失效内容脚本的消息监听，处理同步和异步通信失败，并在后台计时页面就绪后发送配置。浏览器错误页中的历史记录可在更新及刷新网页后清除。

已有 localhost 自定义地址保留为兼容标识，跳转时映射到当前扩展的内置页；不用手改扩展 ID。确认新版可用后可以停用旧服务及自启动项，详细步骤见 README。

## 验收

1. 打开受限网站，应进入扩展内置理由页（地址以 `chrome-extension://` 开头），输入框和确定按钮同时显示。
2. 前 5 秒不能提交；之后不足 5 个非空白字符仍不能放行。
3. 输入合格理由后按 Enter 或点击“确定”，直接进入原网站，没有中间倒计时页面。
4. B站搜索页切换到首页或其他子域名，再新开抖音标签，都应共享原放行。
5. 两组倒计时开关应关闭，网页不出现剩余时间计时框。
6. 到期后应再次门禁。

自动测试覆盖实际后台、内容脚本和理由页逻辑；Chrome API 使用测试替身，不能替代实际浏览器验收：

```powershell
node --test --test-isolation=none .\scripts\test-shared-session.cjs
node .\scripts\defaults.cjs --check
node .\scripts\package.cjs
```
