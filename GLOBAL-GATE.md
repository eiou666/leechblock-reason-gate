# 全局共享放行修复

之前的油猴脚本只处理标签页继承，不能满足全局放行。新版直接修改 LeechBlock 的后台：同一 Block Set 使用同一个放行截止时间，与新标签来源、网站子域名、导航方式无关。

## 已安装用户：更新到单页等待版（1.7.3.2）

在 `chrome://extensions` 找到 **LeechBlock NG — 全局理由门（本地版）**，点击卡片上的重新加载按钮，然后刷新已经打开的门禁页。无需重新安装或修改拦截页 URL；原时段和配置保留，但扩展重新加载会清除当前临时放行。

页面现在始终同时显示输入框与「确定」按钮。进入页面后等待 5 秒，期间按钮可见但点击无反应，也不能用 Ctrl+Enter 提前提交；不显示任何数字倒计时。5 秒后按钮可点击，理由合格时直接放行，不再刷新进入单独的倒计时页面。只等待不会自动放行。刷新门禁页会重新等待 5 秒。

理由校验和等待由本地扩展的内容脚本统一处理；若没有重新加载扩展，新的按钮可能一直不可点击。延迟仍读取对应 Block Set 的 delaySecs（当前两组都是 5 秒，单页模式至少等待 5 秒）。

## 需要手动完成的一次安装

1. 在油猴里关闭之前的 **LeechBlock - 站内新标签继承剩余时间** 脚本（`leechblock-inherit-tabs.user.js`），之后刷新旧网页。
2. 在 Chrome 地址栏输入 `chrome://extensions`，关闭原来的 **LeechBlock NG**。只需关闭，不要卸载，原配置保留以便恢复。
3. 打开右上角「开发者模式」，点击「加载已解压的扩展程序」，选择文件夹：

   下载/克隆的仓库目录下的 `leechblock-shared` 子文件夹。

4. 确认 **LeechBlock NG — 全局理由门（本地版）** 已启用，原版仍处于关闭状态。两者不能同时启用，否则原版仍会重复拦截。
5. 打开本地版「选项」，核对已经自动预置的两组规则，再刷新 B 站页面。通常不需要重新填写或导入配置。

扩展安装和开关步骤需要手动操作。首次安装完整步骤（包括 Python 服务）请参见 [README](README.md)。自动测试不能代替在自己的浏览器里验收。

## 已预置规则

| Block Set | 每天生效时段 | 每次全局放行 |
| --- | --- | --- |
| 1 | 07:00–11:50、12:00–17:50、18:00–22:00 | 30 分钟 |
| 2 | 00:00–07:00、11:50–12:00、17:50–18:00、22:00–24:00 | 5 分钟 |

两组都要求至少 5 个非空白字符的理由，在同一页隐式等待 5 秒后手动确认。包含抖音、B站、YouTube、X/Twitter、知乎及已配置的子域名和短链接域名。理由页 URL 与本机端口 8765 服务保持不变。

**同一组的所有网站共享放行**：例如在 B 站放行后，同组内的 YouTube、知乎等也共享剩余时间，而不只是 B 站子域名。

- 从搜索页点首页、从首页点收藏/空间、直接输入网址、书签、中键、新窗口，都读取同一组截止时间。
- 新标签、新导航和重复完成的倒计时不会把剩余时间重新补满。
- 到期后再次门禁；切换到另一组时段时，旧组放行立即结束。例如 11:40 放行，最多持续到 11:50，然后适用 5 分钟组。
- 午夜仍属于同一个 5 分钟组，23:59 的放行可持续到次日 00:04。
- Chrome 后台工作线程休眠不会丢失放行；浏览器重启、扩展重新加载/更新会清除放行，需要重新过门禁。
- 普通窗口和无痕窗口分开计时。无痕浏览仍需手动允许此扩展在无痕中运行。
- 开着的页面也会在到期后被拦截（沿用原扩展约每秒一次的检查；浏览器调度可能有延迟）。
- 更改该组设置会使旧放行失效；临时锁定仍优先。弹出菜单里的 Discard remaining time 可结束当前组的全局放行。

## 验收

在理由页输入至少 5 个非空白字符：前 5 秒点击按钮应无反应，5 秒后点击应直接进入目标网站，不显示倒计时或中间页面。然后依次测试：

1. `https://search.bilibili.com/all` →「首页」。
2. B站首页顶部 →「收藏」/个人空间。
3. 新开一个空白标签，手动进入 B站收藏地址或 YouTube。

在同一个剩余放行时间内，上述路径均不应再次显示理由框，倒计时也不应重新开始。如果仍重复，优先检查是否同时开启了原版 LeechBlock。

## 更新、备份和恢复

本地版保留原配置界面，但使用独立的扩展 ID 和设置。需要迁移额外的旧配置时，可以先在原版导出 TXT，再导入本地版并保存；不要把运行中的原版和本地版同时打开。全局功能识别的是 `http://127.0.0.1:8765/lb-custom/reason-gate.html?$S&$U`，不要更改为别的拦截页。

这是基于 LeechBlock NG 1.7.3 的本地修改版，不是官方更新；不会自动接收商店更新，需要后续手动维护。保留此目录，Chrome 从这里加载代码。

如需回退：关闭本地版，重新开启原版。原版源文件和原配置没有被修改；新版不使用旧油猴脚本，也不将它作为安装依赖。

测试命令：

```powershell
node --test --test-isolation=none .\scripts\test-shared-session.cjs
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\test.ps1
```

自动测试执行真实的 `background.js` 拦截函数、`blocked.js` 倒计时和共享存储逻辑，Chrome API 用测试替身模拟，不等同于真实浏览器验收。
